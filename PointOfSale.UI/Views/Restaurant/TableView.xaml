<UserControl x:Class="PointOfSale.UI.Views.Restaurant.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale.UI.Views.Restaurant"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Table Management" Style="{StaticResource HeaderTextStyle}"/>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="285"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Form -->
            <Border Style="{StaticResource PanelCardStyle}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Table Form" Style="{StaticResource SubHeaderTextStyle}"/>

                    <!-- Table Name -->
                    <TextBlock Text="Table Name:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Name="NameTextBox"
                             Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                             MaxLength="50"
                             CharacterCasing="Upper"
                             Style="{StaticResource TextBoxStyle}" 
                             PreviewKeyDown="MoveFocusOnEnter"/>

                    <!-- Capacity -->
                    <TextBlock Text="Capacity:" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding Capacity, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                             MaxLength="2"
                             Style="{StaticResource TextBoxStyle}"
                             PreviewKeyDown="MoveFocusOnEnter"
                             GotFocus="SelectAllText_GotFocus"/>

                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,24,0,0" HorizontalAlignment="Center">
                        <Button Name="SubmitButton"
                                Content="{Binding SaveButtonText}"
                                Command="{Binding SaveCommand}" 
                                Style="{StaticResource PrimaryButtonStyle}" 
                                Click="SubmitButton_Click"
                                Margin="0,0,12,0"/>

                        <Button Content="Edit" 
                                Command="{Binding EditCommand}" 
                                Style="{StaticResource SecondaryButtonStyle}"/>

                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Right Panel - Data Grid -->
            <Border Grid.Column="1" Style="{StaticResource PanelCardStyle}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#F1F2F6" Padding="12,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Table List" 
                                       Style="{StaticResource SubHeaderTextStyle}" 
                                       VerticalAlignment="Center"
                                       Margin="0"/>

                            <Button Grid.Column="1"
                                    Command="{Binding RefreshCommand}" 
                                    Style="{StaticResource RefreshButtonStyle}" 
                                    ToolTip="Refresh Table List"
                                    Margin="10,0,0,0" />

                            <Button Grid.Column="3"
                                    Command="{Binding DeleteCommand}" 
                                    Style="{StaticResource DeleteButtonStyle}" 
                                    ToolTip="Delete Table"/>

                        </Grid>
                    </Border>

                    <!-- Data Grid -->
                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding TableList}" 
                              SelectedItem="{Binding SelectedTable, UpdateSourceTrigger=PropertyChanged}" 
                              AutoGenerateColumns="False"
                              SelectionMode="Single"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Style="{StaticResource DataGridStyle}"
                              Margin="0,4,0,0">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" Visibility="Hidden"/>
                            <DataGridTextColumn Header="Table Name" Binding="{Binding Name}" Width="Auto" />
                            <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" Width="Auto">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Current Status" Binding="{Binding CurrentStatus}" Width="Auto"/>
                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
