<UserControl x:Class="PointOfSale.UI.Views.Restaurant.MenuItemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:PointOfSale.UI.Views.Restaurant"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Menu Items" Style="{StaticResource HeaderTextStyle}"/>

        <Border Grid.Row="1" Margin="0,0,0,4" Style="{StaticResource PanelCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="Category" Style="{StaticResource LabelStyle}" />
                    <ComboBox ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding SelectedCategory}"
                              SelectedValuePath="Id"
                              IsEditable="True"
                              IsTextSearchEnabled="True"
                              TextSearch.TextPath="DisplayName"
                              ItemTemplate="{StaticResource HierarchicalCategoryTemplate}"
                              Width="220"
                              Style="{StaticResource ComboBoxStyle}"
                              Margin="0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock Text="Station" Style="{StaticResource LabelStyle}" />
                    <ComboBox ItemsSource="{Binding Stations}"
                              SelectedItem="{Binding SelectedStation, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Id"
                              IsEditable="True"
                              IsTextSearchEnabled="True"
                              TextSearch.TextPath="Name"
                              Width="180"
                              Style="{StaticResource ComboBoxStyle}"
                              Margin="0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="Menu Item Name" Style="{StaticResource LabelStyle}" />
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource SearchTextBoxStyle}"
                             Width="250"
                             Margin="0"/>
                </StackPanel>

                <Button Grid.Column="3" 
                        Command="{Binding SearchCommand}" 
                        Content="Search"
                        Style="{StaticResource SearchButtonStyle}"
                        VerticalAlignment="Bottom"
                        Height="32" Width="32"/>

                <Button Grid.Column="5" 
                        Command="{Binding RefreshCommand}" 
                        Content="Refresh List"
                        Style="{StaticResource RefreshButtonStyle}"
                        VerticalAlignment="Bottom"
                        Height="32" Width="32"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding FilteredMenuItems}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowHeight="50"
                  Style="{StaticResource DataGridStyle}">

            <DataGrid.Columns>

                <DataGridTemplateColumn Header="Image" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="50" Height="50" CornerRadius="4" ClipToBounds="True" Background="#F5F5F5"
                                    VerticalAlignment="Center" HorizontalAlignment="Center" Margin="4">
                                <Grid>
                                    <TextBlock Text="IMG" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#DDD" FontSize="10"/>
                                    <Image Source="{Binding ImageUrl, IsAsync=True, TargetNullValue={x:Null}, FallbackValue={x:Null}}" 
                                           Stretch="UniformToFill"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4,4"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Item Name" Binding="{Binding Name}" Width="*" FontWeight="SemiBold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4,4"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                

                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4,4"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Category" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource BrandBrush}" BorderThickness="1" Background="#E3F2FD" CornerRadius="4" Padding="8,4" 
                                    HorizontalAlignment="Left" VerticalAlignment="Center">
                                <TextBlock Text="{Binding CategoryName}" Foreground="{StaticResource BrandBrush}" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Station" Binding="{Binding TargetStation}" Width="120"/>

                <DataGridTextColumn Header="Base Price" Binding="{Binding Price, StringFormat='Rs. {0:N2}'}" Width="120" HeaderStyle="{StaticResource RightAlignedDataGridColumnHeaderStyle}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Taxes" Binding="{Binding AppliedTaxes}" Width="160">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4,4"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Status" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" Background="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}" 
                                    CornerRadius="4" Padding="8,4" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusTextConverter}}" 
                                           FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Action" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                                <Button
                                        ToolTip="Edit Menu Item Details"
                                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding DataContext.CanEditMenuItem, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BoolToVisibility}}"
                                        Style="{StaticResource EditButtonStyle}"
                                        Margin="0,0,6,0"/>

                                <Button
                                        ToolTip="Delete Menu Item"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding Id}"
                                        Visibility="{Binding DataContext.CanDeleteMenuItem, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BoolToVisibility}}"
                                        Style="{StaticResource DeleteButtonStyle}"
                                        Margin="0,0,6,0"/>

                                <Button
                                        ToolTip="Quick Price Update"
                                        Command="{Binding DataContext.QuickUpdatePriceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Visibility="{Binding DataContext.CanSetPrice, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BoolToVisibility}}"
                                        Style="{StaticResource PriceButtonStyle}"/>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Margin="0,4,0,0" Style="{StaticResource PanelCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Records:" FontWeight="SemiBold" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Text="{Binding TotalCount}" Margin="5,0,0,0" FontWeight="Bold" Style="{StaticResource LabelStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="{Binding ActiveCount, StringFormat='Active: {0}'}" Style="{StaticResource LabelStyle}" Foreground="{StaticResource SuccessBrush}" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding InactiveCount, StringFormat='Inactive: {0}'}" Style="{StaticResource LabelStyle}" Foreground="{StaticResource DangerBrush}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.RowSpan="4" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Width="50" Height="50" IsIndeterminate="True"/>
        </Border>
    </Grid>

</UserControl>
