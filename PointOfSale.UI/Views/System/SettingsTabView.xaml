<UserControl x:Class="PointOfSale.UI.Views.Settings.SettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="900">
    <Grid>

        <TabControl Style="{StaticResource TabControlStyle}"
                    SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">

            <TabItem Header="Company" Style="{StaticResource TabItemStyle}"
                     Visibility="{Binding CanNavCompany, Converter={StaticResource BoolToVisibility}}"
                     Margin="0 0 0 4">
                <ContentControl Content="{Binding CompanySettingsViewModel}"/>
            </TabItem>

            <TabItem Header="Tax Configuration" Style="{StaticResource TabItemStyle}" 
                     Margin="0 0 0 4"
                     Visibility="{Binding CanNavTax, Converter={StaticResource BoolToVisibility}}">
                <ContentControl Content="{Binding TaxConfigurationViewModel}"/>
            </TabItem>

            <TabItem Header="Bank Registration" Style="{StaticResource TabItemStyle}" Margin="0 0 0 4"
                     Visibility="{Binding CanNavBank, Converter={StaticResource BoolToVisibility}}" >
                <ContentControl Content="{Binding BankViewModel}"/>
            </TabItem>

            <TabItem Header="Payment Terminal" Style="{StaticResource TabItemStyle}" 
                     Margin="0 0 0 4"
                     Visibility="{Binding CanNavPaymentTerminal, Converter={StaticResource BoolToVisibility}}">
                <ContentControl Content="{Binding PaymentTerminalViewModel}"/>
            </TabItem>

            <TabItem Header="Account Mapping" Style="{StaticResource TabItemStyle}" Margin="0 0 0 4"
                     Visibility="{Binding CanNavAccountMapping, Converter={StaticResource BoolToVisibility}}">
                <ContentControl Content="{Binding AccountMappingViewModel}"/>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
