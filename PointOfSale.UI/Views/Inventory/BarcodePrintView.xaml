<UserControl x:Class="PointOfSale.UI.Views.Inventory.BarcodePrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale.UI.Views.Inventory"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Barcode Print" Style="{StaticResource HeaderTextStyle}"/>

        <Border Grid.Row="1" Style="{StaticResource PanelCardStyle}" Margin="0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Margin="0,0,0,8">
                    <TextBlock Text="Search Product to Print:" Style="{StaticResource LabelStyle}"/>

                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" 
                                VerticalContentAlignment="Center"
                                Style="{StaticResource TextBoxStyle}"
                                Width="220"
                                Margin="0">
                            
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>

                        <Button Content="Search" 
                                Command="{Binding SearchCommand}" 
                                Style="{StaticResource SearchButtonStyle}"
                                Margin="5,0"/>
                        
                    </StackPanel>

                    <ListBox ItemsSource="{Binding SearchResults}" BorderThickness="1" BorderBrush="{StaticResource Brush.Card.Border}" Padding="12 8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ProductCode}" FontWeight="Bold" Width="80" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ProductName}" Width="200" VerticalAlignment="Center"/>
                                    <Button Content="Add" Command="{Binding DataContext.AddToQueueCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Margin="10,0"
                                            Style="{StaticResource SecondaryButtonStyle}"/>
                                    
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                </StackPanel>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding PrintQueue}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          Style="{StaticResource DataGridStyle}">

                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Code" Binding="{Binding ProductCode}" IsReadOnly="True" Width="120"/>
                        <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding SellingPrice, StringFormat=N2}" IsReadOnly="True" Width="100"/>
                        <DataGridTextColumn Header="Copies" Binding="{Binding QtyToPrint, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    </DataGrid.Columns>

                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Print Selected Labels" 
                            Command="{Binding PrintAllCommand}" 
                            Style="{StaticResource SecondaryButtonStyle}"/>

                </StackPanel>
            </Grid>
            
        </Border>
        
        
    </Grid>

</UserControl>
